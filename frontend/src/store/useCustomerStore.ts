import { create } from "zustand";

// Currently selected customer for detail view
interface useCustomerStoreState {
  // TODO: define state shape
}

export const useCustomerStore = create<useCustomerStoreState>(() => ({
  // TODO: initial state
}));
